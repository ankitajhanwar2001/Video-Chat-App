<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Video Chat App</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://kit.fontawesome.com/c939d0e917.js"></script>
    </head>
    <body>

        <nav class="navbar navbar-inverse" style="position: fixed; width: 100%;">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#">Video Chat App</a>
                </div>
                <ul style="position: absolute; left: 160px;" class="nav navbar-nav">
                    <li class="active"><a href="/">Home</a></li>
                </ul>
                <% if(newUser) { %>
                  <ul style="position: absolute; left: 230px;" class="nav navbar-nav" style="padding-left: 2px;">
                      <li class="active"><a href="/<%= newUser._id %>/join">Join</a></li>
                  </ul>
                <% } %>
                <ul class="nav navbar-nav navbar-right">
                  <% if(!newUser) { %>
                      <li><a href="/register"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                      <li><a href="/login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                  <% } else { %>

                    <li>
                      <a href="/myprofile/<%= newUser._id %>">
                        <!-- <span class="glyphicon glyphicon-user"></span> -->
                        <img src="<%= newUser.profileUrl %>" style="width: 20px; height: 20px; border-radius: 50%; box-shadow: none;">
                        <%= newUser.username %>
                      </a>
                    </li>
                    <li><a href="/logout"><span class="glyphicon glyphicon-log-out"></span>Logout</a></li>
                  <% } %>
                </ul>
            </div>
        </nav>

        <div style="padding-top: 70px; position: relative;" class="container">
            <% if(error && error.length>0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length>0) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>

        <style>
            body {
                background-image:url(https://wallpapercave.com/wp/bYp71HX.jpg);
            }
        </style>
