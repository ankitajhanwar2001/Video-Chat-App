<!DOCTYPE html>
<html>
<head>
  <title>Video Chat App</title>
  <link rel="stylesheet" href="./../profile.css">
</head>
  <body>
    <h2 style="color: white; text-align:center; font-size: 40px;">My Profile</h2>
    <div class="card">
      <img src="<%= user.profileUrl %>" alt="<%= user.firstname %> <%= user.lastname %>">
      <h1><%= user.firstname %> <%= user.lastname %></h1>
      <p class="title">Username: <%= user.username %></p>
      <p>Email: <%= user.email %></p>
      <a id="edit-btn" class="btn btn-success" href="/profile/<%= user.id %>/edit">Edit</a>
      <a id="edit-btn" class="btn btn-success" href="/">Home</a>
    </div>
  </body>
</html>
